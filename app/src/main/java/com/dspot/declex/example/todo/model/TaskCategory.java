package com.dspot.declex.example.todo.model;

public enum TaskCategory {
    /*please don't alter this order,
    otherwise categories previously stored in DB won't be correctly interpreted*/
    OTHER, SHOP, SPORT, PLACE, EVENT, GYM
}
