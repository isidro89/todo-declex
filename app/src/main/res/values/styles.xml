<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar.FullScreen" parent="AppTheme.NoActionBar">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.Text.Label" parent="AppTheme">
        <item name="android:textSize">12dp</item>
        <item name="android:fontFamily">@font/lato_black</item>
        <item name="android:textColor">#33181743</item>
    </style>

    <style name="AppTheme.Text.Input" parent="Base.Widget.MaterialComponents.TextInputEditText">
        <item name="android:padding">8dp</item>
        <item name="android:textSize">12dp</item>
        <item name="android:fontFamily">@font/lato_regular</item>
        <item name="android:textColor">#181743</item>
    </style>

    <style name="AppTheme.Text.Title" parent="AppTheme">
        <item name="android:padding">8dp</item>
        <item name="android:textSize">20dp</item>
        <item name="android:fontFamily">@font/baloo_regular</item>
        <item name="android:textColor">#181743</item>
        <item name="android:textAllCaps">true</item>
    </style>


</resources>
